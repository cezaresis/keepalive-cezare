name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 * * *'   # todo dia às 12:00 UTC (09:00 no horário de Brasília)
  workflow_dispatch:         # permite rodar manualmente pelo botão "Run workflow"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase para manter o projeto ativo
        run: |
          curl -s "https://wyslllzgyywykijkwydn.supabase.co/rest/v1/clientes?select=id&limit=1" \
            -H "apikey: sb_publishable_HtfULQcDRItxRjVvbE-IxQ_DqbMT6jA" \
            -H "Authorization: Bearer sb_publishable_HtfULQcDRItxRjVvbE-IxQ_DqbMT6jA"
